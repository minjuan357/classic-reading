zhipu:
  api:
    base-url: https://open.bigmodel.cn/api/paas/v4
    api-key: 8cd1f8bea5c041a092ab69db43470060.T6h0rTk4pBUDLUk4
    model: glm-4-flash   # 确保模型名称正确（不要带 v）

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/classic_reading?useSSL=false&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8mb4&characterSetResults=utf8mb4&connectionCollation=utf8mb4_unicode_ci&sessionVariables=character_set_client=utf8mb4,character_set_connection=utf8mb4,character_set_results=utf8mb4
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-init-sql: "SET NAMES utf8mb4 COLLATE utf8mb4_unicode_ci"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  thymeleaf:
    cache: false

server:
  port: 8080